# 运行数据(含密钥的 config.json、SQLite、生成的图片)
/data/

# 编译产物
/stcs
/stcs.exe

# 前端
web/node_modules/
web/dist/
web/*.tsbuildinfo

# 原 Python 版(仅本地参考,不纳入仓库)
GPT和Gemini生图带压测/

# Python 版画质/超分检测(已用 Go 重写到 internal/inspect;仅本地参考,不推 github)
GPT生图画质超分检测/

# 杂项
*.log
.DS_Store
